package Transport.OOP;

public class Intercity extends Train{
    // Poyezd && Train
    public Intercity(String name, double price, double length, String fuelType) {
        super(name, price, length, fuelType);
    }

    public Intercity() {
    }

}
